spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project_manager
    username: postgres
    password: 2019Vad2019
    driver-class-name: org.postgresql.Driver
  thymeleaf:
    cache: false
  mail:
    host: localhost
    port: 1025
    username:
    password:
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

app:
  mail:
    from: no-reply@project-manager.local
